# 2026-04-03 이후 주간보고 요약

- 작성 시각: 2026-04-07
- 범위: 2026-04-03 ~ 2026-04-07
- 기준: `git log --since=2026-04-03` 커밋 및 변경 파일

---

## 1) 한 줄 요약

이번 주는 Pipeline/Vivado 시뮬레이션 흐름 정리, TFLM용 테스트벤치 확장, pointwise 계열 bare-metal 실행 방식 정리, 그리고 pointwise_light 경량 케이스 0/1/2의 RTL 검증 완료까지 진행했다.

---

## 2) 날짜별 진행 내용

### 2026-04-03

- `9c14cf0` `Vivado 시뮬레이션 플로우 개선 및 파일 정리`
- 주요 작업:
  - Pipeline 및 Single_cycle의 Vivado 시뮬레이션 실행 흐름을 정리
  - `run_vivado_entry.tcl`, `run_vivado_sim.tcl`, `run_vivado_sim.bat` 계열 파일을 정비
  - Single_cycle 쪽 테스트벤치와 가이드 문서 정리
  - 시뮬레이션 관련 파일 배치를 정돈하여 실행 진입점을 단순화

### 2026-04-06

- `86fea8a` `TFLM 테스트벤치 및 시뮬레이션 스크립트 추가`
- 주요 작업:
  - Pipeline `testbench_TFLM`와 관련 실행 스크립트 추가
  - Bootcode 기반/비기반 시뮬레이션을 분리해서 다룰 수 있는 기반 마련
  - `.gitignore` 및 `run.bat`, `run_sim.tcl` 파일을 함께 정리

- `c47bebb` `테스트벤치에 스택 초기화 파라미터 추가 및 IMEM/DMEM 메모리 파일 분리 생성 기능 구현`
- 주요 작업:
  - 테스트벤치에 스택 초기화 관련 파라미터 추가
  - IMEM/DMEM 메모리 파일을 분리 생성하는 흐름 구현
  - pointwise 빌드 스크립트와 TB가 동일한 메모리 구조를 참조하도록 정리

- `69b1c23` `build: 부트코드 없는 포인트와이즈 빌드를 위한 링커 스크립트 추가 및 스크립트 수정`
- 주요 작업:
  - `custom_memory_noboot.ld` 추가
  - bootcode 없이 진입하는 pointwise 빌드/실행을 지원하도록 링크 구성을 수정
  - 빌드 산출물 생성 흐름을 테스트벤치와 맞춤

- `cb49bce` `CPU_RESET_PC 파라미터 추가 및 테스트벤치에서 초기화 값 설정`
- 주요 작업:
  - `RV32I_System`과 CPU RTL에 `CPU_RESET_PC` 파라미터 추가
  - 테스트벤치에서 초기 PC를 외부에서 제어할 수 있도록 수정
  - bootcode 유무에 따라 시작 주소를 맞출 수 있는 구조 마련

- `c742113` `테스트벤치의 TFLM 시뮬레이션을 위한 메모리 파일 및 파라미터 추가, 시뮬레이션 스크립트 수정`
- 주요 작업:
  - TFLM 시뮬레이션용 메모리 파일을 추가
  - TB 파라미터와 run script를 연동해 TFLM 경로를 안정화
  - 메모리 초기화와 시뮬레이션 실행 방식의 일관성을 확보

- `a104ee1` `build: 포인트와이즈 빌드에 대한 기본 함수 사용 중지 옵션 추가`
- 주요 작업:
  - pointwise 빌드에서 기본 함수 사용을 끌 수 있는 옵션 추가
  - bare-metal 환경에서 불필요한 runtime 의존성을 줄이는 방향으로 정리

- `94f08be` `main_pointwise.cc에서 결과 처리 방식을 수정하여, a0 레지스터에 결과를 저장하고 무한 루프에 진입하도록 변경함. 부트코드 없는 엔트리에서의 복귀 주소 유효성 문제를 해결하기 위해 self-loop 형태로 정지하도록 구현.`
- 주요 작업:
  - 연산 결과를 a0 레지스터에 저장하도록 변경
  - 부트코드가 없는 엔트리에서 return 주소 문제를 피하기 위해 `jal x0,0` self-loop 방식으로 종료
  - RTL 검증 시 안정적으로 종료 상태를 판별할 수 있게 정리

- `a59d979` `main_pointwise.cc 및 build_host.sh 파일 수정`
- 주요 작업:
  - x86 host 빌드/실행 스크립트와 pointwise 소스를 함께 조정
  - host 결과와 RTL 결과를 비교하기 쉽게 실행 구조를 정리

- `4b908b8` `testbench: 런타임 통계 활성화 및 디버그 레벨 설정 추가`
- 주요 작업:
  - TFLM 테스트벤치에서 runtime stats 출력과 debug level 설정을 활성화
  - 시뮬레이션 중 상태 추적과 디버깅 편의성 향상

### 2026-04-07

- `f83d310` `docs 정리`
- 주요 작업:
  - 기존 Pipeline/TFLM 관련 문서들을 정리
  - 진행 중인 작업과 검증 결과를 문서 체계에 반영

- `8ae8276` `pointwise_light 경량 검증 케이스 추가 및 빌드 스크립트 작성`
- 주요 작업:
  - 경량 pointwise 타깃 `pointwise_light` 추가
  - CASE_ID 기반 0/1/2 검증 케이스 작성
  - `build_pointwise_light.sh` 추가
  - TB에서 DMEM 덤프를 확인할 수 있도록 구조를 확장

- `a315e4e` `pointwise_light 케이스 0/1/2 검증 전체 보고서 추가`
- 주요 작업:
  - case 0/1/2의 기대값과 RTL 결과를 전체 대조한 상세 보고서 작성
  - a0 checksum 및 DMEM 덤프 기준으로 PASS 확인

---

## 3) 이번 주 핵심 성과

- Pipeline/Vivado 시뮬레이션 실행 흐름을 정리해 테스트 진입점을 단순화했다.
- TFLM 테스트벤치에 메모리 파일 분리, stack 초기화, reset PC 제어, runtime stats 출력 기능을 추가해 검증 환경을 강화했다.
- pointwise 계열을 bootcode 없이도 안정적으로 돌릴 수 있도록 링커 스크립트와 종료 방식을 정리했다.
- pointwise_light 경량 케이스 0/1/2를 추가하고 RTL 결과를 전체 검증하여 동작 일치를 확인했다.

---

## 4) 검증 결과

- `./build_pointwise_light.sh 2` 실행 완료, 종료 코드 0 확인
- `pointwise_light` case 0/1/2 전체 검증 보고서 작성 완료
- 검증 기준:
  - `x10(a0)` checksum
  - `[TB][DMEM]` dump 영역 128 bytes 전체
- 결과:
  - case 0 PASS
  - case 1 PASS
  - case 2 PASS

관련 상세 문서:

- [pointwise_light 경량 진행 요약](20260407_0039_[TFLM]pointwise_light_pipeline_progress_summary.md)
- [pointwise_light case 0/1/2 검증 전체 보고서](20260407_0102_[TFLM]pointwise_ligt_case0_1_2_verification_full_report.md)

---

## 5) 비고

- 현재까지의 변경은 주로 TFLM 검증 인프라와 pointwise 계열 실행 구조 안정화에 집중되어 있다.
- 다음 주에는 pointwise_light 검증 자동화 확장, 추가 케이스 분리, 그리고 TB의 pass/fail 자동 판정을 더 강화할 수 있다.